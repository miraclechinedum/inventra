<x-app-layout title="Sales">
    <div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-sm font-semibold text-[#0b56c9]">Transactions</p><h1 class="mt-1 text-3xl font-bold">Sales</h1><p class="mt-2 text-slate-600">Completed and voided sale history.</p></div>@can('create', \App\Models\Sale::class)<a href="{{ route('sales.create') }}" class="rounded-xl bg-[#0b56c9] px-5 py-3 font-semibold text-white">Record sale</a>@endcan</div>
    <form method="GET" class="mt-7 grid gap-3 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm md:grid-cols-4"><input name="search" value="{{ is_string(request('search')) ? request('search') : '' }}" placeholder="Sale or customer" class="rounded-xl border border-slate-300 px-3 py-2"><select name="status" class="rounded-xl border border-slate-300 px-3"><option value="">All statuses</option>@foreach(\App\Enums\SaleStatus::cases() as $status)<option value="{{ $status->value }}" @selected(is_string(request('status')) && request('status') === $status->value)>{{ ucfirst($status->value) }}</option>@endforeach</select><select name="payment_method" class="rounded-xl border border-slate-300 px-3"><option value="">All methods</option>@foreach(\App\Enums\PaymentMethod::cases() as $method)<option value="{{ $method->value }}" @selected(is_string(request('payment_method')) && request('payment_method') === $method->value)>{{ $method->label() }}</option>@endforeach</select><select name="payment_status" class="rounded-xl border border-slate-300 px-3"><option value="">All payment states</option>@foreach(\App\Enums\PaymentStatus::cases() as $payment)<option value="{{ $payment->value }}" @selected(is_string(request('payment_status')) && request('payment_status') === $payment->value)>{{ ucfirst($payment->value) }}</option>@endforeach</select><input type="date" name="from" value="{{ is_string(request('from')) ? request('from') : '' }}" class="rounded-xl border border-slate-300 px-3 py-2"><input type="date" name="to" value="{{ is_string(request('to')) ? request('to') : '' }}" class="rounded-xl border border-slate-300 px-3 py-2">@if(!$salesRep)<select name="sold_by" class="rounded-xl border border-slate-300 px-3"><option value="">All staff</option>@foreach($sellers as $seller)<option value="{{ $seller->id }}" @selected(is_string(request('sold_by')) && request('sold_by') === (string)$seller->id)>{{ $seller->name }}</option>@endforeach</select>@else<div></div>@endif<button class="rounded-xl bg-slate-900 px-4 py-2 font-semibold text-white">Filter</button></form>
    <div class="mt-6 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="px-5 py-4">Sale</th><th class="px-5 py-4">Customer</th><th class="px-5 py-4">Items</th><th class="px-5 py-4">Total</th><th class="px-5 py-4">Payment</th><th class="px-5 py-4">Status</th><th class="px-5 py-4">Sold by</th><th></th></tr></thead><tbody class="divide-y">@forelse($sales as $sale)<tr><td class="px-5 py-4"><p class="font-mono font-semibold">{{ $sale->sale_number }}</p><p class="text-xs text-slate-500">{{ $sale->created_at->format('M j, Y g:i A') }}</p></td><td class="px-5 py-4">{{ $sale->customer_name_snapshot }}</td><td class="px-5 py-4">{{ $sale->items_count }}</td><td class="px-5 py-4 font-semibold">₦{{ \App\Support\Money::format($sale->total_amount) }}</td><td class="px-5 py-4">{{ $sale->payment_method->label() }} · {{ ucfirst($sale->payment_status->value) }}</td><td class="px-5 py-4">{{ ucfirst($sale->status->value) }}</td><td class="px-5 py-4">{{ $sale->sold_by_name_snapshot }}</td><td class="px-5 py-4"><a href="{{ route('sales.show', $sale) }}" class="font-semibold text-[#0b56c9]">View</a></td></tr>@empty<tr><td colspan="8" class="px-6 py-16 text-center text-slate-500">No sales match these filters.</td></tr>@endforelse</tbody></table></div>@if($sales->hasPages())<div class="border-t p-4">{{ $sales->links() }}</div>@endif</div>
</x-app-layout>
