<x-app-layout :title="$sale->sale_number.' activity'"><a href="{{ route('sales.show',$sale) }}" class="text-sm font-semibold text-[#0b56c9]">← Sale</a><h1 class="mt-3 text-3xl font-bold">Sale activity</h1><div class="mt-7 divide-y rounded-2xl border border-slate-200 bg-white shadow-sm">@forelse($events as $event)<article class="p-5"><div class="flex justify-between"><p class="flex items-center gap-2 font-semibold"><span class="ui-feed-ordinal">{{ $events->firstItem() + $loop->index }}</span>{{ str($event->action)->replace('_',' ')->title() }}</p><time class="text-sm text-slate-500">{{ $event->created_at->format('M j, Y g:i A') }}</time></div><p class="mt-1 text-sm text-slate-600">Actor: {{ $event->actor?->name ?? 'System' }}</p></article>@empty<p class="p-12 text-center text-slate-500">No sale activity recorded.</p>@endforelse @if($events->hasPages())<x-table-footer :paginator="$events" noun="event" />@endif</div></x-app-layout>
