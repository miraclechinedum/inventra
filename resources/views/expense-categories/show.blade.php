<x-app-layout title="Expense Category"><div class="flex justify-between"><div><a class="text-blue-700" href="{{ route('expense-categories.index') }}">← Categories</a><h2 class="mt-2 text-3xl font-bold">{{ $category->name }}</h2><p class="font-mono">{{ $category->category_code }}</p></div><a class="rounded-xl border px-4 py-3" href="{{ route('expense-categories.edit', $category) }}">Edit</a></div><div class="mt-6 rounded-2xl border bg-white p-6"><p class="whitespace-pre-line">{{ $category->description ?: 'No description.' }}</p><p class="mt-3">Status: {{ $category->is_active ? 'Active' : 'Inactive' }}</p><form class="mt-4" method="POST" action="{{ route($category->is_active ? 'expense-categories.deactivate' : 'expense-categories.activate', $category) }}">@csrf<button class="rounded-xl border px-4 py-2">{{ $category->is_active ? 'Deactivate' : 'Activate' }}</button></form></div><h3 class="mt-8 text-xl font-bold">Historical Expenses</h3><div class="mt-3 rounded-2xl border bg-white p-6">@forelse($expenses as $expense)<p><a class="text-blue-700" href="{{ route('expenses.show', $expense) }}">{{ $expense->expense_number }}</a> · {{ $expense->incurred_at->format('d M Y') }} · ₦{{ \App\Support\Money::format($expense->amount) }}</p>@empty<p>No Expenses recorded.</p>@endforelse</div><div class="mt-4">{{ $expenses->links() }}</div></x-app-layout>
