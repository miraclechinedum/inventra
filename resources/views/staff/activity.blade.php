<x-app-layout title="Staff activity"><a href="{{ route('staff.show', $staffMember) }}" class="text-sm font-semibold text-[#0b56c9]">← {{ $staffMember->name }}</a><h1 class="mt-3 text-3xl font-bold">Security activity</h1><p class="mt-2 text-slate-600">Recent account and administrative events for {{ $staffMember->name }}.</p><div class="mt-6 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="divide-y divide-slate-100">@forelse ($events as $event)<article class="flex flex-wrap justify-between gap-3 px-6 py-5"><div><p class="flex items-center gap-2 font-semibold"><span class="ui-feed-ordinal">{{ $events->firstItem() + $loop->index }}</span>{{ str($event->event)->replace('_', ' ')->title() }}</p><p class="mt-1 text-sm text-slate-500">Actor: {{ $event->actor?->name ?? 'System or account owner' }} · IP: {{ $event->ip_address ?? 'Not recorded' }}</p></div><time class="text-sm text-slate-500">{{ $event->created_at->format('M j, Y g:i A') }}</time></article>@empty<p class="px-6 py-16 text-center text-slate-500">No security activity has been recorded.</p>@endforelse</div><x-table-footer :paginator="$events" noun="event" /></div></x-app-layout>
