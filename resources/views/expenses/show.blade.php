<x-app-layout title="Expense"><div class="flex justify-between"><div><a class="text-blue-700" href="{{ route('expenses.index') }}">← Expenses</a><h2 class="mt-2 font-mono text-3xl font-bold">{{ $expense->expense_number }}</h2></div><a class="rounded-xl border px-4 py-3" href="{{ route('expenses.receipt', $expense) }}">Print voucher</a></div><div class="mt-6 grid gap-3 rounded-2xl border bg-white p-6"><p><strong>Category:</strong> {{ $expense->category_name_snapshot }} ({{ $expense->category_code_snapshot }})</p><p><strong>Amount:</strong> ₦{{ \App\Support\Money::format($expense->amount) }}</p><p><strong>Payment method:</strong> {{ $expense->payment_method->label() }}</p><p><strong>Payee:</strong> {{ $expense->payee ?: '—' }}</p><p><strong>Reference:</strong> {{ $expense->reference_number ?: '—' }}</p><p class="whitespace-pre-line"><strong>Description:</strong> {{ $expense->description }}</p><p class="whitespace-pre-line"><strong>Internal note:</strong> {{ $expense->note ?: '—' }}</p><p><strong>Incurred:</strong> {{ $expense->incurred_at->format('d M Y') }}</p><p><strong>Recorded by:</strong> {{ $expense->recorded_by_name_snapshot }} on {{ $expense->created_at->format('d M Y H:i') }}</p></div></x-app-layout>
