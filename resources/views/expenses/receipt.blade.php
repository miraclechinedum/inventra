<!doctype html><html lang="{{ str_replace('_', '-', app()->getLocale()) }}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>{{ $expense->expense_number }} · Expense Voucher</title>@vite(['resources/css/app.css', 'resources/js/app.js'])</head><body class="ui-standalone-receipt"><button type="button" data-print-page class="mb-6 rounded-lg bg-[#0b56c9] px-4 py-2 font-semibold text-white print:hidden">Print</button><x-receipt-letterhead document="Expense Voucher" /><h2 class="mt-2 text-xl">Expense Voucher · {{ $expense->expense_number }}</h2><div class="mt-6 grid gap-3 border-y py-5"><p>Date: {{ $expense->incurred_at->format('d M Y') }}</p><p>Category: {{ $expense->category_name_snapshot }} ({{ $expense->category_code_snapshot }})</p><p>Description: {{ $expense->description }}</p><p>Payee: {{ $expense->payee ?: '—' }}</p><p>Payment Method: {{ $expense->payment_method->label() }}</p><p>Reference: {{ $expense->reference_number ?: '—' }}</p><p>Recorded By: {{ $expense->recorded_by_name_snapshot }}</p></div><p class="mt-6 text-right text-2xl font-bold">Amount: ₦{{ \App\Support\Money::format($expense->amount) }}</p>@livewireScriptConfig</body></html>
