<x-app-layout title="Customer Refunds"><div class="flex justify-between"><h1 class="text-3xl font-bold">Customer Refunds</h1><a class="text-blue-700" href="{{ route('returns.index') }}">View Returns</a></div><form class="mt-5 grid gap-3 rounded-2xl border bg-white p-4 md:grid-cols-6"><input name="search" value="{{ $filters['search'] }}" placeholder="Refund or Sale number" class="rounded-xl border-slate-300"><input type="date" name="from" value="{{ $filters['from'] }}" class="rounded-xl border-slate-300"><input type="date" name="to" value="{{ $filters['to'] }}" class="rounded-xl border-slate-300"><input name="customer" value="{{ $filters['customer'] }}" placeholder="Customer ID" class="rounded-xl border-slate-300"><select name="method" class="rounded-xl border-slate-300"><option value="">All methods</option><option value="cash" @selected($filters['method']==='cash')>Cash</option><option value="transfer" @selected($filters['method']==='transfer')>Bank transfer</option></select><button class="rounded-xl bg-slate-800 text-white">Filter</button></form><div class="mt-6 overflow-x-auto rounded-2xl border bg-white"><table class="w-full text-left text-sm"><tbody>@forelse($rows as $refund)<tr class="border-t"><td class="p-4"><a class="text-blue-700" href="{{ route('refunds.show',$refund) }}">{{ $refund->refund_number }}</a><br>{{ $refund->refunded_at->format('d M Y H:i') }}</td><td>{{ $refund->sale_number_snapshot }}<br>{{ $refund->customer_name_snapshot }}</td><td>₦{{ \App\Support\Money::format($refund->amount) }}</td><td>{{ $refund->payment_method->label() }}</td><td>{{ $refund->refunded_by_name_snapshot }}</td></tr>@empty<tr><td class="p-8">No refunds recorded.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $rows->links() }}</div></x-app-layout>
