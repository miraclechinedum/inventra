<x-app-layout title="Sales Returns"><div class="flex justify-between"><h1 class="text-3xl font-bold">Sales Returns</h1></div><div class="mt-6 overflow-x-auto rounded-2xl border bg-white"><table class="w-full text-left text-sm"><thead><tr><th class="p-4">Return</th><th>Sale / Customer</th><th>Value</th><th>Items</th><th>Recorded by</th></tr></thead><tbody>@forelse($rows as $return)<tr class="border-t"><td class="p-4"><a class="text-blue-700" href="{{ route('returns.show',$return) }}">{{ $return->return_number }}</a><br>{{ $return->returned_at->format('d M Y H:i') }}</td><td>{{ $return->sale_number_snapshot }}<br>{{ $return->customer_name_snapshot }}</td><td>₦{{ \App\Support\Money::format($return->merchandise_value) }}<br>Receivable reduction ₦{{ \App\Support\Money::format($return->receivable_reduction) }}</td><td>{{ $return->items_count }}</td><td>{{ $return->returned_by_name_snapshot }}</td></tr>@empty<tr><td colspan="5" class="p-8 text-center">No returns recorded.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $rows->links() }}</div></x-app-layout>
