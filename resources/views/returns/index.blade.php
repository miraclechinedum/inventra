<x-app-layout title="Sales Returns"><div class="flex justify-between"><h1 class="text-3xl font-bold">Sales Returns</h1><a class="text-blue-700" href="{{ route('refunds.index') }}">View Refunds</a></div><form method="GET" class="ui-filter-bar mt-5 grid gap-3 rounded-2xl border bg-white p-4 md:grid-cols-6"><input aria-label="Search records" name="search" value="{{ $filters['search'] }}" placeholder="Return or Sale number" class="rounded-xl border-slate-300"><input aria-label="From date" type="date" name="from" value="{{ $filters['from'] }}" class="rounded-xl border-slate-300"><input aria-label="To date" type="date" name="to" value="{{ $filters['to'] }}" class="rounded-xl border-slate-300"><input aria-label="Customer" name="customer" value="{{ $filters['customer'] }}" placeholder="Customer ID" class="rounded-xl border-slate-300"><input aria-label="Staff" name="staff" value="{{ $filters['staff'] }}" placeholder="Staff ID" class="rounded-xl border-slate-300"><button class="rounded-xl bg-slate-800 text-white">Filter</button></form><div class="mt-2 text-right"><x-filter-reset /></div><div class="mt-6 overflow-x-auto rounded-2xl border bg-white"><table class="w-full text-left text-sm"><thead><tr><th class="ui-sn">S/N</th><th class="p-4">Return</th><th>Sale / Customer</th><th>Value</th><th>Items</th><th>Recorded by</th></tr></thead><tbody>@forelse($rows as $return)<tr class="border-t"><td class="ui-sn">{{ $rows->firstItem() + $loop->index }}</td><td class="p-4"><a class="text-blue-700" href="{{ route('returns.show',$return) }}">{{ $return->return_number }}</a><br>{{ $return->returned_at->format('d M Y H:i') }}</td><td>{{ $return->sale_number_snapshot }}<br>{{ $return->customer_name_snapshot }}</td><td>₦{{ \App\Support\Money::format($return->merchandise_value) }}<br>Receivable reduction ₦{{ \App\Support\Money::format($return->receivable_reduction) }}</td><td>{{ $return->items_count }}</td><td>{{ $return->returned_by_name_snapshot }}</td></tr>@empty<tr><td colspan="6" class="p-8 text-center"><x-empty-state title="No returns recorded." description="Try another search or adjust your filters." /></td></tr>@endforelse</tbody></table></div><x-table-footer :paginator="$rows" noun="return" /></x-app-layout>
