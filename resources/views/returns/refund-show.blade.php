<x-app-layout :title="$refund->refund_number"><div class="flex justify-between"><div><a class="text-blue-700" href="{{ route('refunds.index') }}">← Refunds</a><h1 class="mt-2 text-3xl font-bold">{{ $refund->refund_number }}</h1></div><a class="rounded-xl bg-blue-700 px-4 py-3 text-white" href="{{ route('refunds.receipt',$refund) }}">Refund receipt</a></div><div class="mt-6 rounded-2xl border bg-white p-6"><p>Sale {{ $refund->sale_number_snapshot }}</p><p>{{ $refund->customer_code_snapshot }} · {{ $refund->customer_name_snapshot }}</p><p class="mt-4 text-2xl font-bold">₦{{ \App\Support\Money::format($refund->amount) }}</p><p>{{ $refund->payment_method->label() }} · {{ $refund->reason }}</p><p>Refunded by {{ $refund->refunded_by_name_snapshot }} on {{ $refund->refunded_at->format('d M Y H:i') }}</p></div></x-app-layout>
