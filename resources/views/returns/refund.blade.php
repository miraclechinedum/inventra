<x-app-layout title="Record Refund"><a class="text-blue-700" href="{{ route('sales.show',$sale) }}">← {{ $sale->sale_number }}</a><h1 class="mt-2 text-3xl font-bold">Record Refund</h1><div class="mt-5 rounded-2xl border bg-white p-5"><p>Payments received: ₦{{ \App\Support\Money::format($state['payments']) }}</p><p>Returned merchandise: ₦{{ \App\Support\Money::format($state['returns']) }}</p><p>Previous refunds: ₦{{ \App\Support\Money::format($state['refunds']) }}</p><p class="font-bold">Currently refundable: ₦{{ \App\Support\Money::format($state['credit']) }}</p></div><form method="POST" action="{{ route('sales.refunds.store',$sale) }}" class="mt-5 space-y-4 rounded-2xl border bg-white p-6">@csrf<input type="hidden" name="request_token" value="{{ $requestToken }}"><x-validation-errors /><input name="amount" required placeholder="Refund amount" class="w-full rounded-xl border-slate-300"><select name="payment_method" class="w-full rounded-xl border-slate-300"><option value="cash">Cash</option><option value="transfer">Bank transfer</option></select><input name="reason" required maxlength="500" placeholder="Reason" class="w-full rounded-xl border-slate-300"><input name="note" maxlength="500" placeholder="Internal note" class="w-full rounded-xl border-slate-300"><button class="rounded-xl bg-red-700 px-5 py-3 font-semibold text-white">Record immutable Refund</button></form></x-app-layout>
