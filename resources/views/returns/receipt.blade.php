<x-app-layout :receipt="true" :title="$return->return_number"><section data-print-page class="mx-auto max-w-3xl bg-white p-8"><x-receipt-letterhead document="Return Receipt" /><p>{{ $return->return_number }} · Sale {{ $return->sale_number_snapshot }}</p><p>{{ $return->customer_code_snapshot }} · {{ $return->customer_name_snapshot }}</p>@foreach($return->items as $item)<p class="mt-3">{{ $item->product_name_snapshot }} · {{ $item->quantity_returned }} {{ $item->unit_snapshot }} · ₦{{ \App\Support\Money::format($item->return_line_value) }}</p>@endforeach<p class="mt-5 font-bold">Total ₦{{ \App\Support\Money::format($return->merchandise_value) }}</p><p>Recorded by {{ $return->returned_by_name_snapshot }} · {{ $return->returned_at->format('d M Y H:i') }}</p><button type="button" data-print-trigger class="receipt-controls mt-6 rounded bg-blue-700 px-4 py-2 text-white">Print</button><x-receipt-footer /></section></x-app-layout>
