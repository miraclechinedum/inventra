<x-app-layout title="WhatsApp delivery logs">
    <div class="flex flex-wrap justify-between gap-4"><div><h1 class="text-3xl font-bold">WhatsApp receipt deliveries</h1><p class="mt-2 text-slate-600">Append-oriented manual receipt delivery history.</p></div><span class="rounded-full px-3 py-2 text-sm font-semibold {{ $configured ? 'bg-emerald-100 text-emerald-800' : 'bg-amber-100 text-amber-800' }}">{{ $configured ? 'Provider configured' : 'Provider not configured' }}</span></div>
    <div class="mt-6 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full divide-y text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="ui-sn">S/N</th><th class="px-5 py-4">Sale</th><th>Destination</th><th>Status</th><th>Attempt</th><th>Requested by</th><th>Date</th><th></th></tr></thead><tbody class="divide-y">@forelse($deliveries as $delivery)<tr><td class="ui-sn">{{ $deliveries->firstItem() + $loop->index }}</td><td class="px-5 py-4 font-mono">{{ $delivery->sale->sale_number }}</td><td>{{ \App\Support\PhoneMask::display($delivery->destination_phone) }}</td><td><x-status-badge :status="$delivery->status" /></td><td>{{ $delivery->attempt }}</td><td>{{ $delivery->creator->name }}</td><td>{{ $delivery->requested_at->format('M j, Y g:i A') }}</td><td><a class="font-semibold text-[#0b56c9]" href="{{ route('whatsapp.deliveries.show', $delivery) }}">View</a></td></tr>@empty<tr><td colspan="8" class="px-6 py-16 text-center text-slate-500"><x-empty-state title="No receipt deliveries recorded." description="Try another search or adjust your filters." /></td></tr>@endforelse</tbody></table></div><x-table-footer :paginator="$deliveries" noun="delivery" /></div>
</x-app-layout>
