@csrf @if(($method ?? null) === 'PUT') @method('PUT') @endif
<div class="grid gap-5 sm:grid-cols-2"><x-form-field label="Product name" name="name" :value="old('name', $product->name ?? '')" required /><x-form-field label="SKU" name="sku" :value="old('sku', $product->sku ?? '')" required /><div><label class="text-sm font-semibold">Category</label><select name="category_id" class="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3" required>@foreach($categories as $category)<option value="{{ $category->id }}" @selected((string) old('category_id', $product->category_id ?? '') === (string) $category->id)>{{ $category->name }}</option>@endforeach</select>@error('category_id')<p class="mt-2 text-sm text-red-600">{{ $message }}</p>@enderror</div><div><label class="text-sm font-semibold">Unit</label><select name="unit" class="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3">@foreach(\App\Enums\ProductUnit::cases() as $unit)<option value="{{ $unit->value }}" @selected(old('unit', isset($product) ? $product->unit->value : 'piece') === $unit->value)>{{ $unit->label() }}</option>@endforeach</select></div><x-form-field label="Cost price (₦)" name="cost_price" inputmode="decimal" :value="old('cost_price', $product->cost_price ?? '')" required /><x-form-field label="Selling price (₦)" name="selling_price" inputmode="decimal" :value="old('selling_price', $product->selling_price ?? '')" required /><x-form-field label="Reorder level" name="reorder_level" inputmode="decimal" :value="old('reorder_level', $product->reorder_level ?? '0')" required />@if(!isset($product))<x-form-field label="Initial stock" name="initial_stock" inputmode="decimal" :value="old('initial_stock', '0')" required />@endif<div class="sm:col-span-2"><label class="text-sm font-semibold">Description</label><textarea name="description" rows="4" class="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3">{{ old('description', $product->description ?? '') }}</textarea>@error('description')<p class="mt-2 text-sm text-red-600">{{ $message }}</p>@enderror</div></div><div class="mt-7 flex gap-3"><button class="rounded-xl bg-[#0b56c9] px-5 py-3 font-semibold text-white">{{ $submitLabel }}</button><a href="{{ route('inventory.index') }}" class="rounded-xl px-5 py-3 font-semibold text-slate-600">Cancel</a></div>
