<x-app-layout title="Inventory">
    <div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-sm font-semibold uppercase tracking-[.18em] text-[#0b56c9]">Catalog</p><h1 class="mt-2 text-3xl font-bold">Inventory</h1><p class="mt-2 text-slate-600">Search products and monitor current stock.</p></div>@can('create', \App\Models\Product::class)<div class="flex gap-2"><a href="{{ route('inventory.categories.index') }}" class="rounded-xl border border-slate-300 bg-white px-4 py-3 font-semibold">Categories</a><a href="{{ route('inventory.products.create') }}" class="rounded-xl bg-[#0b56c9] px-5 py-3 font-semibold text-white">Add product</a></div>@endcan</div>
    <form method="GET" class="ui-filter-bar mt-7 grid gap-3 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm sm:grid-cols-5"><input aria-label="Search records" name="search" value="{{ is_string(request('search')) ? request('search') : '' }}" placeholder="Product or SKU" class="rounded-xl border border-slate-300 px-4 py-2.5"><select aria-label="Category" name="category" class="rounded-xl border border-slate-300 px-3"><option value="">All categories</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(is_string(request('category')) && request('category') === (string) $category->id)>{{ $category->name }}</option>@endforeach</select><select aria-label="Stock level" name="stock" class="rounded-xl border border-slate-300 px-3"><option value="">All stock</option><option value="low" @selected(is_string(request('stock')) && request('stock') === 'low')>Low stock</option><option value="ok" @selected(is_string(request('stock')) && request('stock') === 'ok')>Stock OK</option></select>@if($canManage)<select aria-label="Status" name="status" class="rounded-xl border border-slate-300 px-3"><option value="">All statuses</option><option value="active" @selected(is_string(request('status')) && request('status') === 'active')>Active</option><option value="inactive" @selected(is_string(request('status')) && request('status') === 'inactive')>Inactive</option></select>@else<div></div>@endif<button class="rounded-xl bg-slate-900 px-4 py-2.5 font-semibold text-white">Filter</button></form>
<div class="mt-2 text-right"><x-filter-reset /></div>
    <div class="mt-6 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs uppercase tracking-wide text-slate-500"><tr><th class="ui-sn">S/N</th><x-sort-header key="sku" label="SKU" :active="$sort['key']" :direction="$sort['direction']" /><x-sort-header key="name" label="Product" :active="$sort['key']" :direction="$sort['direction']" /><th class="px-5 py-4">Category</th><x-sort-header key="price" label="Selling price" :active="$sort['key']" :direction="$sort['direction']" /><x-sort-header key="stock" label="Stock" :active="$sort['key']" :direction="$sort['direction']" /><x-sort-header key="reorder" label="Reorder level" :active="$sort['key']" :direction="$sort['direction']" /><x-sort-header key="status" label="Status" :active="$sort['key']" :direction="$sort['direction']" /><th></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($products as $product)<tr class="{{ $product->isLowStock() ? 'bg-amber-50' : '' }}"><td class="ui-sn">{{ $products->firstItem() + $loop->index }}</td><td class="px-5 py-4 font-mono font-semibold">{{ $product->sku }}</td><td class="px-5 py-4 font-semibold">{{ $product->name }}</td><td class="px-5 py-4 text-slate-600">{{ $product->category->name }}</td><td class="px-5 py-4">₦{{ \App\Support\Money::format($product->selling_price) }}</td><td class="px-5 py-4 font-semibold {{ $product->isLowStock() ? 'text-amber-800' : '' }}">{{ rtrim(rtrim($product->current_stock, '0'), '.') }} {{ $product->unit->value }}</td><td class="px-5 py-4">{{ rtrim(rtrim($product->reorder_level, '0'), '.') }}</td><td class="px-5 py-4"><x-status-badge :status="$product->is_active ? 'active' : 'inactive'" />@if($product->isLowStock())<span class="ml-2 rounded-full bg-amber-200 px-2 py-1 text-xs font-bold text-amber-900">Low stock</span>@endif</td><td class="px-5 py-4"><a href="{{ route('inventory.products.show', $product) }}" class="font-semibold text-[#0b56c9]">View</a></td></tr>@empty<tr><td colspan="9" class="px-6 py-16 text-center text-slate-500"><x-empty-state title="No products match these filters." description="Try another search or adjust your filters." /></td></tr>@endforelse</tbody></table></div><x-table-footer :paginator="$products" noun="product" /></div>
</x-app-layout>
