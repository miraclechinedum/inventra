<x-app-layout title="Receive Stock"><div class="mx-auto max-w-5xl"><h2 class="text-2xl font-bold">Receive Purchase</h2><p class="text-slate-500">Stock is increased immediately when this record is submitted.</p><form class="mt-6 grid gap-5 rounded-2xl border bg-white p-6" method="POST" action="{{ route('purchases.store') }}">@csrf<input type="hidden" name="request_token" value="{{ $requestToken }}"><label class="grid gap-2 font-semibold">Supplier<select class="rounded-xl border-slate-300" name="supplier_id" required><option value="">Select supplier</option>@foreach($suppliers as $supplier)<option value="{{ $supplier->id }}">{{ $supplier->supplier_code }} · {{ $supplier->name }}</option>@endforeach</select></label><label class="grid gap-2 font-semibold">Supplier reference<input class="rounded-xl border-slate-300" name="reference_number"></label><label class="grid gap-2 font-semibold">Note<textarea class="rounded-xl border-slate-300" name="note"></textarea></label><h3 class="font-bold">Items</h3>@for($i=0;$i<5;$i++)<div class="grid gap-3 md:grid-cols-3"><select class="rounded-xl border-slate-300" name="items[{{ $i }}][product_id]"><option value="">Product</option>@foreach($products as $product)<option value="{{ $product->id }}">{{ $product->sku }} · {{ $product->name }} ({{ $product->unit->value }})</option>@endforeach</select><input class="rounded-xl border-slate-300" name="items[{{ $i }}][quantity]" placeholder="Quantity" inputmode="decimal"><input class="rounded-xl border-slate-300" name="items[{{ $i }}][unit_cost]" placeholder="Unit cost" inputmode="decimal"></div>@endfor @if($errors->any())<div class="text-red-600"><ul>@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif<button class="inventra-primary-action w-fit">Confirm Stock Receipt</button></form></div></x-app-layout>
